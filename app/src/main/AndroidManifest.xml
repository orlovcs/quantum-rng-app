<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.orlovcs.reaction">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:label="Quantum Dice"
            android:name=".dice_activity"
            android:parentActivityName=".MainActivity"
            >
        </activity>

        <activity
            android:label="Quantum Numbers"
            android:name=".rng_activity"
            android:parentActivityName=".MainActivity"
            >
        </activity>

        <activity
            android:label="Quantum Lottery"
            android:name=".lottery_activity"
            android:parentActivityName=".MainActivity"
            >
        </activity>

        <activity
            android:label="Quantum Coins"
            android:name=".coin_activity"
            android:parentActivityName=".MainActivity"
            >
        </activity>

        <activity
            android:label="Quantum Password"
            android:name=".password_activity"
            android:parentActivityName=".MainActivity"
            >
        </activity>

        <activity
            android:label="Quantum Numbers"
            android:name=".rng_activity"
            android:parentActivityName=".MainActivity"
            >
        </activity>

        <activity
            android:label="About"
            android:name=".about_activity"
            android:parentActivityName=".MainActivity"
            >
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>
    <uses-permission android:name="android.permission.INTERNET" />

</manifest>